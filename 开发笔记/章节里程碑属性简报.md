# 章节里程碑属性简报

> 本文档对 `scripts/story/StoryProgress.gd` 中五个章节的全部里程碑进行属性简报。
> 实现状态：✅ 已实现  |  ❌ 未实现（无对应任务/触发）

---

## 第一章：博客启蒙期（2002-2005）

> 所有 milestone 初始值 `true`（开局即完成），已全部有对应实现。

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 奖励/效果 | 备注 |
|---|--------|------|----------|----------|-----------|------|
| 1 | `blog_online` | ✅ | `scenes/s004/s_004.gd:25` 开局场景直接调用 `set_completed` | 故事开局场景自动执行 | 无（自动标记） | 非 TaskManager 路径 |
| 2 | `first_article_posted` | ✅ | `post_event` + 条件 `first_post_eq_1` | 发布第一篇博文 → 解锁初始任务 + 启动时间 | ✅ 解锁 16 个日常任务、开游戏时钟 | 核心开关 |
| 3 | `sousuo_indexed` | ✅ | `post_event` + 条件文章数≥10 | SEO 通知弹窗 → 升级奖励 | ✅ SEO 等级提升 | — |
| 4 | `blog_union_joined` | ✅ | `time_check` + SEO==100 | 弹窗通知 → 加入联盟 → 更新按钮状态 | ✅ 加入博客江湖 | — |
| 5 | `first_friend_link` | ✅ | `friendlink_added` 信号 | 友链添加 → 弹窗 | ✅ SEO+访问量提升提示 | main.gd 转发信号 |
| 6 | `rss_enabled` | ✅ | `blogger.gd:856` 每日结算检测 RSS>0 时调用 `TaskManager._on_rss_first_subscriber` | `check_tasks_by_trigger("rss_subscribe", ...)` → 弹窗 + 里程碑 | ✅ 里程碑标记 + 弹窗 | 直接方法调用（非 Godot 信号） |
| 7 | `first_article_favorited` | ✅ | `blogger.gd:807` 每日结算检测收藏>0 时调用 `TaskManager._on_article_first_favorited` | `check_tasks_by_trigger("article_favorited", ...)` → 弹窗 + 里程碑 | ✅ 里程碑标记 + 弹窗 | 直接方法调用（非 Godot 信号） |
| 8 | `first_income` | ✅ | `ad_income_paid` 信号 | 收到广告佣金 → 弹窗 + 里程碑 | ✅ 弹窗提示收益加成规则 | main.gd 连接信号 |
| 9 | `icp_filing_done` | ✅ | `icp_filing_complete` 信号 | 备案完成 → 弹窗 + 设置备案号 | ✅ 合法运营资质 | — |
| 10 | `award_2005` | ✅ | `AwardManager.process_award()` | 排名判定 → 至少提名才标记 | ✅ 称号「博客新星/新锐/之星」 | 动态结果 |
| 11 | `year_summary_2005` | ✅ | `post_event`（年度总结发布在2005年）或 `time_check`（2005年末防遗漏） | 双路径：正常发布触发 + 年末防遗漏兜底 | ✅ 进入四年回顾场景 `review.tscn` | 有 fallback 机制 |

### 第一章完成奖励
`main.gd:_on_chapter_reward` → 写作+5、技术+5、等级+5、金钱+1000

---

## 第二章：博客黄金期（2005-2010）

> 全部初始值 `false`（需玩家推进）。

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 奖励/效果 | 备注 |
|---|--------|------|----------|----------|-----------|------|
| 1 | `literature_weekly` | ✅ | `post_event` + 文学能力值≥40 + 发布“文学周刊” | 发布文学周刊 → 弹窗 + 写作+5 | ✅ 写作能力+5 | — |
| 2 | `code_weekly` | ✅ | `post_event` + 编程能力值≥40 + 发布“程序员周刊” | 发布程序员周刊 → 弹窗 + 技术+5 | ✅ 技术能力+5 | — |
| 3 | `rss_100` | ✅ | `time_check` + 条件 RSS≥100 + 未完成 | 定期检查 → 弹窗 | ✅ 里程碑标记 | — |
| 4 | `income_1000` | ✅ | `time_check` + 条件累计收益≥1000 | 定期检查 → 弹窗 | ✅ 里程碑标记 | — |
| 5 | `award_2010` | ✅ | `AwardManager.process_award()` | 同 award_2005 逻辑，第2届 | ✅ 动态称号 | AwardManager 自动处理 |
| 6 | `year_summary_2010` | ✅ | `post_event`（2010年度总结发布）或 `time_check`（2010年末防遗漏） | 双路径：正常发布 + fallback | ✅ 进入五年回顾场景 | 任务 id: `chapter2_end_2010_review` / `chapter2_end_2010_review_fallback` |

### 第二章完成奖励
同第一章通用奖励模板（写作+5、技术+5、等级+5、金钱+1000）

---

## 第三章：博客转型期（2010-2015）

> 全部初始值 `false`。

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 奖励/效果 | 备注 |
|---|--------|------|----------|----------|-----------|------|
| 1 | `viral_article` | ✅ | `post_event` + 发布“爆款网文” | 发布爆款网文 → 弹窗 | ✅ 里程碑标记 + 弹窗 | 文学能力≥60 解锁 |
| 2 | `advanced_tutorial` | ✅ | `post_event` + 发布“深度技术” | 发布深度技术文章 → 弹窗 | ✅ 里程碑标记 + 弹窗 | 编程能力≥60 解锁 |
| 3 | `philosophy_critique` | ✅ | `post_event` + 发布“哲学批判” | 发布哲学批判 → 弹窗 | ✅ 里程碑标记 + 弹窗 | 文学能力≥60 解锁 |
| 4 | `geek_frontier` | ✅ | `post_event` + 发布“极客前沿” | 发布极客前沿 → 弹窗 | ✅ 里程碑标记 + 弹窗 | 编程能力≥60 解锁 |
| 5 | `wechat_public` | ✅ | `time_check`（2012年1月）+ 未开通 | 弹窗通知 → 解锁“运营公众号”日程 | ✅ 解锁公众号维护任务 | — |
| 6 | `followers_1000` | ✅ | `time_check` + 粉丝≥1000 | 弹窗 → 开启流量主收入 | ✅ 里程碑标记 + 弹窗 | — |
| 7 | `mobile_adapted` | ✅ | `time_check`（2011年7月）+ 未完成 → 解锁适配任务；`mobile_adapt_complete` 信号完成 | 适配完成后标记 | ✅ 里程碑标记 + 弹窗 | 双阶段（通知+完成） |
| 8 | `https_upgraded` | ✅ | `time_check`（2014年8月）+ 未完成 → 解锁升级任务；`https_upgrade_complete` 信号完成 | HTTPS 升级完成后标记 | ✅ 里程碑标记 + 弹窗 | 双阶段（通知+完成） |
| 9 | `novel_first_post` | ✅ | `post_event` + 发布“小说连载(付费)” | 发布第一篇连载 → 弹窗 | ✅ 里程碑标记 + 弹窗 | 文学能力≥80 解锁 |
| 10 | `novel_batch_complete` | ✅ | `blogger.gd:521` 批次≥100章时调用 `TaskManager._on_novel_batch_complete` | 批次完成 → 弹窗 + 里程碑 | ✅ 里程碑标记 + 弹窗 | 小说连载每100章触发一次 |
| 11 | `novel_ip_authorized` | ✅ | `blogger.gd:629` IP授权成功时调用 `TaskManager._on_novel_ip_authorized` | IP授权成功 → 弹窗 + 里程碑 | ✅ 里程碑标记 + 弹窗 | 每批次仅触发一次 |
| 12 | `hacker_first_post` | ✅ | `post_event` + 发布"黑客攻防(付费)" | 发布第一篇黑客攻防 → 弹窗 | ✅ 里程碑标记 + 弹窗 | 编程能力≥80 解锁 |
| 13 | `hacker_course_complete` | ✅ | `blogger.gd:503` 篇数≥100时调用 `TaskManager._on_hacker_course_complete` | 课程完成 → 弹窗 + 里程碑 | ✅ 里程碑标记 + 弹窗 | 累计100篇触发 |
| 14 | `hacker_course_authorized` | ✅ | `blogger.gd:669` 课程授权成功时调用 `TaskManager._on_hacker_course_authorized` | 授权成功 → 弹窗 + 里程碑 | ✅ 里程碑标记 + 弹窗 | 一次性触发 |
| 11 | `award_2015` | ✅ | `AwardManager.process_award()` | 第3届 | ✅ 动态称号 | — |
| 12 | `year_summary_2015` | ✅ | `post_event`（2015年度总结）或 `time_check`（2015年末防遗漏） | 双路径：正常发布 + fallback | ✅ 进入五年回顾场景 | — |

### 第三章完成奖励
同通用奖励模板

---

## 第四章：博客挑战期（2015-2020）

> 全部初始值 `false`。

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 奖励/效果 | 备注 |
|---|--------|------|----------|----------|-----------|------|
| 1 | `book_published` | ✅ | `book_publish_complete` 触发器，由 `BookPublishManager._complete_book_publish()` 调用 `TaskManager._on_book_publish_complete()` | 出版畅销书累计 100 篇 → 出版社审核 → 正式出版上市 | ✅ 里程碑标记（走 TaskConfig 任务系统，有 `book_published_not_completed` 条件防重复） | 完整流程：写书→编辑→审核→上市 |
| 2 | `open_source_start` | ✅ | `open_source_complete` 触发器，由 `OpenSourceManager._complete_os_project()` 调用 `TaskManager._on_open_source_complete()` | 开源项目累计 100 篇 → 社区运营 → 厂商赞助上线 | ✅ 里程碑标记（走 TaskConfig 任务系统，有 `open_source_start_not_completed` 条件防重复） | 完整流程：开发→社区→审核→赞助 |
| 3 | `book_notes` | ✅ | `post_event` + 发布"出书笔记" | 发布出书笔记 → 里程碑 + 弹窗 | ✅ 里程碑标记 + 弹窗 | 写作能力≥90 解锁 |
| 4 | `os_notes` | ✅ | `post_event` + 发布"开源维护笔记" | 发布开源维护笔记 → 里程碑 + 弹窗 | ✅ 里程碑标记 + 弹窗 | 编程能力≥90 解锁 |
| 5 | `cdn_enabled` | ✅ | `time_check`（2017年6月触发通知）→ `cdn_accelerate_complete`（完成时） | 双阶段：通知解锁网站维护 → 执行CDN加速任务5天 → 完成标记 | ✅ 里程碑标记 + 弹窗 | 类似 `https_upgrade` 模式 |
| 6 | `ai_preview` | ✅ | `time_check`（2019年12月自动触发） | 时间触发弹窗预告 → 直接标记里程碑 | ✅ 里程碑标记 + 弹窗 | 纯剧情事件 |
| 7 | `award_2020` | ✅ | `AwardManager.process_award()` | 第4届 | ✅ 动态称号 | — |
| 8 | `year_summary_2020` | ✅ | `post_event`（2020年度总结）或 `time_check`（2020年末防遗漏） | 双路径：正常发布 + fallback | ✅ 进入五年回顾场景 | — |

### 第四章完成奖励
同通用奖励模板

---

## 第五章：博客重塑期 / 终章（2020-2025）

> 全部初始值 `false`。终章，三主线结局 + 默认结局。

### 结局判定条件

| 结局 | 条件 | 剧情概要 |
|------|------|----------|
| 🖋️ **文学主线** | 累计出版书籍 ≥ 5 | 出版第4本时开始感悟 → 第5本后大彻大悟 → 隐退继承亿万家产 |
| 💻 **编程主线** | 累计开源项目 ≥ 5 且 虫洞算法研究完成 | 第5个项目AI意识萌芽 → 完成贾维斯计划 → 虫洞算法研究突破 → 穿越回25年前 |
| 🎮 **游戏开发** | 出版书籍 ≥ 3 **且** 开源项目 ≥ 3 **且** 游戏开发分支全部完成 | 完成爆款游戏 → 获游戏大奖 → 拒领博客奖 → 隐退 |
| 🏛️ **时代见证者** | 以上均不满足（兜底） | 数字文化遗产永久保存，后辈登门听你讲二十年的故事。你关掉后台面板，泡了杯茶。 |

> 优先级：文学 > 编程 > 游戏 > 默认（确保唯一结局）。
> 大结局触发时间：**2025年11月第4周**（`ending_achieved` 任务触发时根据累计数据分发结局）。

### 公共里程碑（所有路线必达）

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 备注 |
|---|--------|------|----------|----------|------|
| 1 | `ai_unlocked` | ❌ | `time_check`（2023年） | AI 浪潮通知 → 解锁 AI 辅助创作 | 所有路线必经 |
| 2 | `award_2025` | ✅ | `AwardManager.process_award()` 第5届 | 第5届奖项结果，映射到 `fame_let_go` | ✅ 已有实现 |
| 3 | `ending_achieved` | ✅ | 沉思录→无为篇完成 / 贾维斯→虫洞算法完成 | 文学/编程结局弹窗后标记 | 大结局触发器 |
| 4 | `ai_unlocked` | ❌ | `time_check`（2023年） | AI 浪潮通知 → 解锁 AI 辅助创作 | 所有路线必经 |

### 文学主线递进里程碑（累计出书触发，第五章才标记）

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 备注 |
|---|--------|------|----------|----------|------|
| 5 | `philosophy_glimmer` | ✅ | `time_check` + 条件 `book_publish_ge_6` | 哲理苗头 → 解锁「沉思录」子项目 | TaskConfig 的 `unlock_contemplation` 任务 |
| 6 | `philosophy_enlightenment` | ✅ | 沉思录50天完成 | `_complete_contemplation()` 标记里程碑 → 自动解锁「无为篇」日程 | 沉思录完成即触发 |

### 编程主线递进里程碑（累计开源触发，第五章才标记）

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 备注 |
|---|--------|------|----------|----------|------|
| 7 | `ai_consciousness` | ✅ | `time_check` + 条件 `open_source_ge_5` & `time_year_2025` | AI 意识萌芽 → 解锁「贾维斯计划」子项目 | TaskConfig 的 `unlock_jarvis_project` |
| 8 | `ai_research_complete` | ✅ | 贾维斯计划50天完成 | `_complete_jarvis_project()` 标记里程碑 → 自动解锁「虫洞算法研究」日程 | 贾维斯计划完成即解锁虫洞算法研究 |
| 9 | `wormhole_research_complete` | ✅ | 虫洞算法研究50天完成 | `_complete_wormhole_research()` 标记里程碑 → 编程结局弹窗 | 编程结局最终里程碑 |

### 游戏开发分支里程碑（游戏结局路线专属）

| # | 里程碑 | 实现 | 触发方式 | 执行逻辑 | 备注 |
|---|--------|------|----------|----------|------|
| 10 | `game_dev_start` | ✅ | `time_check` + 条件 `book_publish_ge_3` & `open_source_ge_3` | 解锁「游戏开发」子项目 | TaskConfig 的 `unlock_game_dev` 任务 |
| 11 | `game_dev_complete` | ✅ | 游戏开发50天完成 | `_complete_game_dev()` 标记里程碑 → 自动解锁「游戏发布」日程 | 5阶段×10天 |
| 12 | `game_test` | ✅ | 游戏发布第10天 | `add_new_blog_post()` 按天数直接设置 | 测试阶段完成 |
| 13 | `game_trailer` | ✅ | 游戏发布第20天 | `add_new_blog_post()` 按天数直接设置 | 预告发布完成 |
| 14 | `game_released` | ✅ | 游戏发布30天完成 | `_complete_game_release()` 标记里程碑 → 游戏结局弹窗 | 正式发布 |
| 15 | `game_award` | ✅ | 游戏发布30天完成（与released同时） | `_complete_game_release()` 标记 | 获奖大结局 |

> ⚠️ `chapter_completed` 问题：公共+分支合计14个里程碑，但三结局互斥。各分支里程碑不应强制全部完成才能通关。待确认处理方案（后续讨论）。
> 
> 📋 书籍/开源计数器已实装：`BloggerData.gd` 已新增 `book_publish_count` / `open_source_count`，随 `serialize_blogger_data()` 自动存读档。`TaskConfig.gd` 已添加 `book_publish_ge_3/5`、`open_source_ge_3/5` 四个 CUSTOM 条件，可直接用于第五章结局判定。

> 注意：`video_account`、`personal_brand`、`return_to_roots` 已移除——这些概念融合到了结局剧情中作为剧情演出，不作为独立里程碑。

### 第五章完成奖励
同通用奖励模板（仅默认结局触发 `chapter_completed`）

---

## 全局系统说明

### 章节完成奖励弹窗
- 定义：`StoryProgress.chapter_reward_popup(chapter, chapter_name)` 信号
- 处理：`main.gd:_on_chapter_reward()`
- 弹窗预制体：`res://milestones/chapter_reward.tscn`
- 奖励固定：写作+5、技术+5、等级+5、金钱+1000
- 触发条件：章节内所有 milestone = `true`，且仅触发一次（`_emitted_chapters` 防重复）

### 设置里程碑的三种路径

| 路径 | 代码位置 | 说明 |
|------|----------|------|
| TaskManager `_action_set_story_milestone` | `TaskManager.gd:962` | 任务系统动作，附带 `_record_milestone_archive` 记录博客历史事件 |
| AwardManager `try_mark_milestone` | `AwardManager.gd:124` | 奖项评选结果触发，仅当 result ≠ NONE 时标记 |
| 场景直接调用 | `s_004.gd:25` | blog_online 特殊场景直接调用 |

### 开发优先级建议
1. **第四章**：8 个里程碑全部 ✅ 已实现
2. **第五章**：待确认最终里程碑方案后再行开发
